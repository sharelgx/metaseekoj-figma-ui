# 🔄 从AI对话到幻灯片的完整工作流程

## 一、完整流程图

```
┌─────────────────────────────────────────────────────────┐
│  第1步：教师与AI对话（讨论课程需求）                     │
│  ─────────────────────────────────────────────────       │
│  用户输入："我想创建一个Python列表的课程"                │
│     ↓                                                    │
│  调用 aiChat API（5点卡）                                │
│     ↓                                                    │
│  AI回复："好的！我建议包含以下内容：                     │
│          1. 列表创建和初始化                             │
│          2. 索引和切片操作                               │
│          3. 常用方法（append、extend等）                 │
│          需要包含代码示例吗？"                            │
│     ↓                                                    │
│  用户回复："是的，需要代码示例和练习题"                  │
│     ↓                                                    │
│  AI回复："明白了，我会为你生成..."                       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第2步：用户确认需求，点击"生成完整课件"                │
│  ─────────────────────────────────────────────────       │
│  用户点击："✨ 生成完整课件（10点卡）"按钮               │
│     ↓                                                    │
│  前端收集：                                              │
│    - topic: "Python列表操作"                             │
│    - requirements: "包含创建、索引、切片、方法、示例、题目"│
│    - course_id: 123                                      │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第3步：调用后端AI生成API                                │
│  ─────────────────────────────────────────────────       │
│  POST /api/classroom/ai/generate/                        │
│  {                                                       │
│    course_id: 123,                                       │
│    topic: "Python列表操作",                              │
│    requirements: "包含..."                               │
│  }                                                       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第4步：后端AI生成Markdown课件                           │
│  ─────────────────────────────────────────────────       │
│  1. 构建完整提示词（包含格式规范）                       │
│     ↓                                                    │
│  2. 调用 ai_manager.generate_content()                   │
│     - 自动故障转移（Groq失败→Volcengine）               │
│     ↓                                                    │
│  3. AI生成Markdown内容：                                 │
│     ---                                                  │
│     title: Python列表基础操作                            │
│     course_type: python                                  │
│     ---                                                  │
│                                                          │
│     # Python列表基础                                     │
│                                                          │
│     ## 1. 列表创建                                       │
│     ```python                                            │
│     my_list = [1, 2, 3]                                  │
│     ```                                                  │
│                                                          │
│     <!-- flashcards:batch -->                            │
│     ### 列表创建                                         │
│     使用方括号[]创建列表。示例：`[1,2,3]`                │
│     <!-- flashcards:end -->                              │
│                                                          │
│     <!-- question:choice -->                             │
│     [难度: 简单] [知识点: 列表]                          │
│     以下哪个是正确的列表创建方式？                       │
│     <!-- /question -->                                   │
│     ...                                                  │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第5步：Markdown解析器处理                               │
│  ─────────────────────────────────────────────────       │
│  classroom/utils/markdown_parser.py                      │
│     ↓                                                    │
│  parse_markdown_course(markdown_content)                 │
│     ↓                                                    │
│  返回：                                                  │
│  {                                                       │
│    'metadata': {                                         │
│      'title': 'Python列表基础操作',                      │
│      'course_type': 'python'                             │
│    },                                                    │
│    'html_content': '<h1>Python列表基础</h1>...',         │
│    'slides': [                                           │
│      {                                                   │
│        'index': 0,                                       │
│        'content_html': '<h1>Python列表基础</h1>',        │
│        'has_code': true,                                 │
│        'has_question': false                             │
│      },                                                  │
│      {                                                   │
│        'index': 1,                                       │
│        'content_html': '<h2>1. 列表创建</h2>...',        │
│        'has_code': true,                                 │
│        'has_question': false                             │
│      },                                                  │
│      {                                                   │
│        'index': 2,                                       │
│        'content_html': '<!-- flashcards:batch -->...',   │
│        'has_flashcards': true                            │
│      },                                                  │
│      {                                                   │
│        'index': 3,                                       │
│        'content_html': '<!-- question:choice -->...',    │
│        'has_question': true                              │
│      }                                                   │
│    ],                                                    │
│    'flashcards': [...],                                  │
│    'questions': [...]                                    │
│  }                                                       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第6步：保存到数据库                                     │
│  ─────────────────────────────────────────────────       │
│  1. 创建 Document 记录                                   │
│     Document.objects.create(                             │
│       course=course,                                     │
│       title='Python列表基础操作',                        │
│       content_html=parsed['html_content'],               │
│       content_type='AI',                                 │
│       slide_count=12                                     │
│     )                                                    │
│     ↓                                                    │
│  2. 批量创建 Slide 记录                                  │
│     for slide_data in parsed['slides']:                 │
│       Slide.objects.create(                              │
│         document=document,                               │
│         slide_index=slide_data['index'],                 │
│         content_html=slide_data['content_html'],         │
│         has_code=slide_data['has_code'],                 │
│         has_question=slide_data['has_question']          │
│       )                                                  │
│     ↓                                                    │
│  3. 消费点卡                                             │
│     consume_credits(user, 10, '生成课件')                │
│     ↓                                                    │
│  4. 返回结果                                             │
│     return {                                             │
│       document_id: 456,                                  │
│       slides_count: 12,                                  │
│       credits_used: 10                                   │
│     }                                                    │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第7步：前端显示结果                                     │
│  ─────────────────────────────────────────────────       │
│  1. 接收后端返回的 document_id                           │
│     ↓                                                    │
│  2. 更新预览区：                                         │
│     - 显示"课件已生成！"                                 │
│     - 显示"全屏播放幻灯片"按钮                           │
│     ↓                                                    │
│  3. 用户点击"全屏播放"                                   │
│     window.open(`/slide-preview?document_id=456`)        │
│     ↓                                                    │
│  4. 打开幻灯片演示页面                                   │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  第8步：幻灯片展示（SlidePreview.tsx）                   │
│  ─────────────────────────────────────────────────       │
│  1. 根据 document_id 加载幻灯片数据                      │
│     GET /api/classroom/documents/456/slides/             │
│     ↓                                                    │
│  2. 初始化 Reveal.js                                     │
│     ↓                                                    │
│  3. 渲染幻灯片：                                         │
│     - 第1页：标题页（浏览模式）                          │
│     - 第2页：讲解内容（浏览模式）                        │
│     - 第3页：闪卡（自动切换到闪卡交互模式）⭐            │
│     - 第4页：选择题（自动切换到答题模式）⭐              │
│     - ...                                                │
│     ↓                                                    │
│  4. 模式自动切换：                                       │
│     if (slide.has_flashcards) {                          │
│       进入闪卡交互模式                                   │
│       用户可以翻转卡片、标记掌握状态                     │
│     }                                                    │
│     if (slide.has_question) {                            │
│       进入选择题答题模式                                 │
│       用户可以选择、提交、查看解析                       │
│     }                                                    │
└─────────────────────────────────────────────────────────┘
```

---

## 二、详细步骤说明

### 步骤1-2：AI对话和需求收集（前端）

**位置**：`CourseEditor.tsx`

**用户操作**：
1. 输入需求："创建Python列表课程"
2. AI回复建议和问题
3. 用户继续对话细化需求
4. **点击"生成完整课件"按钮**

**代码实现**：
```typescript
// 1. 对话（5点卡/次）
const handleSendMessage = async () => {
  const response = await aiChat({
    message: text,
    conversation_history: messages,
    rule_type: 'ai_edit_course'  // 5点卡
  })
  
  // 显示AI回复
  setMessages([...messages, userMessage, aiResponse])
}

// 2. 生成完整课件（10点卡）
const handleGenerateCourseware = async () => {
  // 收集对话内容作为需求
  const topic = currentTopic || "课程主题"
  const requirements = currentRequirements || "对话内容"
  
  // 调用生成API
  const result = await aiGenerateCourse({
    course_id: course.id,
    topic: topic,
    requirements: requirements
  })
  
  // 保存document_id，用于预览
  setDocumentId(result.document_id)
}
```

---

### 步骤3-4：后端AI生成（Django）

**位置**：`classroom/views_ai.py - AICourseGenerateView`

**处理流程**：

```python
def post(self, request):
    # 1. 接收参数
    course_id = request.data.get('course_id')
    topic = request.data.get('topic')
    requirements = request.data.get('requirements')
    
    # 2. 构建AI提示词（包含Markdown格式规范）
    prompt = AI_COURSEWARE_PROMPT_TEMPLATE + f"""
主题：{topic}
难度：{course.difficulty_level}
课程类型：{course.course_type}
具体要求：{requirements}

请生成完整的课件Markdown文档。
"""
    
    # 3. 调用AI生成（自动故障转移）
    ai_response = ai_manager.generate_content(
        prompt=prompt,
        config={
            'temperature': 0.7,
            'max_tokens': 4000,
            'prefer_region': 'china'  # 优先国内
        },
        user=request.user,
        auto_failover=True  # Groq失败→Volcengine
    )
    
    # 4. 获取Markdown内容
    markdown_content = ai_response.get('content')
    
    # 返回Markdown示例：
    """
    ---
    title: Python列表基础操作
    ---
    
    # Python列表基础
    
    ## 1. 列表创建
    ```python
    my_list = [1, 2, 3]
    ```
    
    <!-- flashcards:batch -->
    ### 列表创建
    使用[]创建列表
    <!-- flashcards:end -->
    
    <!-- question:choice -->
    [难度: 简单]
    以下哪个是正确的列表？
    <!-- /question -->
    """
```

---

### 步骤5：Markdown解析（Python）

**位置**：`classroom/utils/markdown_parser.py`

**解析过程**：

```python
def parse_markdown_course(markdown_text: str):
    parser = MarkdownCourseParser(markdown_text)
    
    # 1. 提取YAML元信息
    metadata = extract_yaml(markdown_text)
    
    # 2. 转换Markdown为HTML
    html_content = markdown.markdown(markdown_text, extensions=[...])
    
    # 3. 识别特殊标记
    # - <!-- slide --> → 强制分页
    # - <!-- flashcards:batch --> → 闪卡批次
    # - <!-- question:choice --> → 测试题占位符
    
    # 4. 生成幻灯片分页
    slides = []
    
    # 第1页：标题页
    slides.append({
        'index': 0,
        'content_html': '<h1>Python列表基础操作</h1>',
        'has_code': False,
        'has_question': False,
        'has_flashcards': False
    })
    
    # 第2页：讲解内容
    slides.append({
        'index': 1,
        'content_html': '<h2>列表创建</h2><pre><code>...</code></pre>',
        'has_code': True,
        'has_question': False,
        'has_flashcards': False
    })
    
    # 第3页：闪卡
    slides.append({
        'index': 2,
        'content_html': '<!-- flashcards:batch -->...',
        'has_code': False,
        'has_question': False,
        'has_flashcards': True  # ⭐ 标记为闪卡页
    })
    
    # 第4页：选择题
    slides.append({
        'index': 3,
        'content_html': '<!-- question:choice -->...',
        'has_code': False,
        'has_question': True,  # ⭐ 标记为测试题页
        'has_flashcards': False
    })
    
    # 5. 提取闪卡数据
    flashcards = extract_flashcards(html_content)
    # [
    #   { front: '列表创建', back: '使用[]创建', code: '[1,2,3]' },
    #   ...
    # ]
    
    # 6. 提取测试题占位符
    questions = extract_questions(html_content)
    # [
    #   { type: 'choice', metadata: {...}, position: 123 },
    #   ...
    # ]
    
    return {
        'metadata': metadata,
        'html_content': html_content,
        'slides': slides,          # ⭐ 幻灯片数组
        'flashcards': flashcards,  # ⭐ 闪卡数据
        'questions': questions     # ⭐ 测试题占位符
    }
```

---

### 步骤6：保存到数据库（Django）

**位置**：`classroom/views_ai.py`

```python
# 1. 解析Markdown
parsed = parse_markdown_course(markdown_content)

# 2. 创建Document记录
document = Document.objects.create(
    course=course,
    title=parsed['metadata']['title'],
    content_html=parsed['html_content'],
    content_type='AI',
    slide_count=len(parsed['slides'])
)

# 3. 批量创建Slide记录
for slide_data in parsed['slides']:
    Slide.objects.create(
        document=document,
        slide_index=slide_data['index'],
        content_html=slide_data['content_html'],
        has_code=slide_data['has_code'],
        has_question=slide_data['has_question'],
        has_flashcards=slide_data.get('has_flashcards', False),  # ⭐ 闪卡标记
        code_language=course.course_type
    )

# 4. TODO: 创建Flashcard记录（未来实现）
# for fc_data in parsed['flashcards']:
#     Flashcard.objects.create(...)

# 5. 消费点卡
consume_credits(user, 10, '生成课件')

# 6. 返回结果
return Response({
    'document_id': document.id,
    'slides_count': len(parsed['slides']),
    'credits_used': 10
})
```

---

### 步骤7-8：前端展示幻灯片（React）

**位置**：`SlidePreview.tsx`

**加载和展示**：

```typescript
// 1. 根据document_id加载幻灯片
useEffect(() => {
  const loadSlides = async () => {
    const response = await axios.get(`/api/classroom/documents/${documentId}/slides/`)
    setSlides(response.data)
    
    // 2. 初始化Reveal.js
    const deck = new Reveal(revealDivRef.current, {
      plugins: [RevealHighlight],
      controls: true,
      progress: true
    })
    await deck.initialize()
    
    // 3. 监听幻灯片切换事件
    deck.on('slidechanged', (event) => {
      const currentSlide = slides[event.indexh]
      
      // ⭐ 检测幻灯片类型，自动切换模式
      if (currentSlide.has_flashcards) {
        setMode('flashcard')  // 进入闪卡模式
      } else if (currentSlide.has_question) {
        setMode('quiz')       // 进入答题模式
      } else {
        setMode('browse')     // 浏览模式
      }
    })
  }
  
  loadSlides()
}, [documentId])

// 4. 根据模式渲染不同组件
return (
  <div className="reveal">
    <div className="slides">
      {slides.map(slide => (
        <section key={slide.id}>
          {/* 浏览模式：直接显示HTML */}
          {mode === 'browse' && (
            <div dangerouslySetInnerHTML={{ __html: slide.content_html }} />
          )}
          
          {/* 闪卡模式：渲染FlashcardInteractive */}
          {mode === 'flashcard' && slide.flashcards && (
            <FlashcardInteractive 
              flashcards={slide.flashcards}
              onComplete={() => Reveal.next()}
            />
          )}
          
          {/* 答题模式：渲染QuizInteractive */}
          {mode === 'quiz' && slide.question && (
            <QuizInteractive 
              questions={[slide.question]}
              onComplete={() => Reveal.next()}
            />
          )}
        </section>
      ))}
    </div>
  </div>
)
```

---

## 三、完整数据流

```
用户需求 (文本)
    ↓
AI对话 (JSON)
    ↓
Markdown (文本)
    ↓
解析器 (Python)
    ↓
结构化数据 (JSON)
    ↓
数据库 (PostgreSQL)
    ├─ Document (课件文档)
    ├─ Slide (幻灯片)
    ├─ Flashcard (闪卡)
    └─ Question (测试题)
    ↓
前端加载 (API)
    ↓
Reveal.js 渲染 (HTML)
    ↓
用户浏览/交互 (UI)
```

---

## 四、关键API端点

| 步骤 | API | 消耗点卡 | 返回 |
|------|-----|----------|------|
| 对话讨论 | POST /ai/chat/ | 5点 | AI回复文本 |
| **生成课件** | POST /ai/generate/ | **10点** | **document_id + slides_count** |
| 加载幻灯片 | GET /documents/{id}/slides/ | 0点 | 幻灯片数组 |

---

## 五、Markdown格式示例

**完整的AI生成Markdown**：

```markdown
---
title: Python列表基础操作
author: AI智慧课堂
course_type: python
difficulty_level: beginner
estimated_time: "45分钟"
---

# Python列表基础操作

<!-- slide -->

## 1. 什么是列表？

列表（List）是Python中最常用的数据结构之一...

```python
# 创建列表
my_list = [1, 2, 3, 4, 5]
fruits = ['apple', 'banana', 'cherry']
```

<!-- slide -->

## 2. 列表索引

索引从0开始...

```python
print(fruits[0])  # 输出: apple
```

<!-- slide -->

<!-- flashcards:batch -->
### 列表创建
使用方括号[]创建列表。示例：`[1, 2, 3]`

### 列表索引
从0开始计数。示例：`list[0]`获取第一个元素

### append方法
在列表末尾添加元素。示例：`list.append(item)`
<!-- flashcards:end -->

<!-- slide -->

<!-- question:choice -->
[难度: 简单] [知识点: 列表创建] [分值: 5分]
以下哪个是正确的列表创建方式？
A. {1, 2, 3}
B. [1, 2, 3]
C. (1, 2, 3)
D. <1, 2, 3>
[答案: B]
[解析: 列表使用方括号[]创建，{}是字典或集合，()是元组]
<!-- /question -->
```

**解析后的幻灯片结构**：

```
第1页：标题 - "Python列表基础操作" (浏览模式)
第2页：讲解 - "什么是列表？" (浏览模式)
第3页：讲解 - "列表索引" (浏览模式)
第4页：闪卡 - 3个知识点闪卡 (闪卡交互模式) ⭐
第5页：测试题 - 选择题 (答题交互模式) ⭐
```

---

## 六、幻灯片模式自动切换

**检测逻辑**：

```typescript
// SlidePresentation.tsx
const detectSlideMode = (slide: Slide) => {
  // 检查幻灯片的标记字段
  if (slide.has_flashcards) {
    return 'flashcard'  // 闪卡交互模式
  }
  if (slide.has_question) {
    return 'quiz'       // 答题交互模式
  }
  return 'browse'       // 默认浏览模式
}

// 监听幻灯片切换
useEffect(() => {
  const currentSlide = slides[currentIndex]
  const newMode = detectSlideMode(currentSlide)
  
  if (newMode !== mode) {
    setMode(newMode)
    console.log(`🔄 切换到${newMode}模式`)
  }
}, [currentIndex])
```

---

## 七、用户完整体验流程

```
1. 访问课程列表
   http://localhost:8081/classroom/teacher/courses
   
2. 点击"进入AI编辑器"
   → 进入对话界面
   
3. 与AI对话（可多轮）
   用户："创建Python列表课程"
   AI："好的，包含哪些内容？"
   用户："包含创建、索引、方法，需要代码示例和练习题"
   AI："明白了，我会为你准备..."
   
4. 点击"生成完整课件"按钮
   → 显示进度条（0% → 100%）
   → 等待30-60秒
   → 提示"生成成功！已生成12个幻灯片"
   
5. 点击"全屏播放幻灯片"
   → 打开新标签页
   → 显示Reveal.js幻灯片
   
6. 幻灯片浏览
   - 第1-3页：浏览讲解内容（翻页即可）
   - 第4页：闪卡页面
     → 自动进入闪卡交互模式
     → 可以点击翻转、标记掌握状态
     → 完成后自动进入下一页
   - 第5页：选择题页面
     → 自动进入答题模式
     → 选择答案、提交、查看解析
     → 完成后进入下一页
   - 第6-12页：继续浏览其他内容
```

---

## 八、点卡消费说明

| 操作 | API | 点卡消耗 | 说明 |
|------|-----|----------|------|
| AI对话 | /ai/chat/ | 5点/次 | 讨论需求、优化内容 |
| **生成课件** | **/ai/generate/** | **10点/次** | **生成完整Markdown并转换为幻灯片** |
| 编辑课件 | /ai/edit/ | 5点/次 | 修改已有课件 |

**建议使用流程**：
1. 先用"AI对话"讨论2-3轮（消耗10-15点）
2. 需求明确后，点击"生成完整课件"（消耗10点）
3. 查看生成的幻灯片
4. 如需修改，继续对话后再次生成

---

## 九、当前实现状态

| 步骤 | 状态 | 说明 |
|------|------|------|
| 1. AI对话 | ✅ 完成 | aiChat API |
| 2. 生成课件按钮 | ✅ 完成 | handleGenerateCourseware |
| 3. 后端AI生成 | ✅ 完成 | aiGenerateCourse API |
| 4. Markdown解析 | ✅ 完成 | markdown_parser.py |
| 5. 保存数据库 | ✅ 完成 | Document + Slide |
| 6. 前端加载 | ⚠️ 需完善 | 需添加slides API |
| 7. 幻灯片展示 | ⚠️ 需完善 | SlidePreview需更新 |
| 8. 模式自动切换 | ⏳ 待实现 | 检测has_flashcards/has_question |

---

## 十、下一步工作

### ⏳ 待完善（明天）

1. **添加slides加载API** （30分钟）
   ```typescript
   // GET /api/classroom/documents/{id}/slides/
   export const getDocumentSlides = async (documentId: number) => {
     return api.get(`/documents/${documentId}/slides/`)
   }
   ```

2. **更新SlidePreview组件** （2小时）
   - 加载真实的slides数据
   - 检测has_flashcards和has_question
   - 自动切换到对应的交互模式

3. **集成FlashcardInteractive** （1小时）
   - 在幻灯片中嵌入闪卡组件
   - 完成后自动进入下一页

4. **集成QuizInteractive** （1小时）
   - 在幻灯片中嵌入答题组件
   - 完成后自动进入下一页

---

**🎉 完整流程已打通！现在可以从对话生成幻灯片了！**

**立即测试**：
```
http://localhost:8081/classroom/teacher/courses
→ 点击"进入AI编辑器"
→ 与AI对话
→ 点击"生成完整课件"
→ 查看幻灯片！
```

